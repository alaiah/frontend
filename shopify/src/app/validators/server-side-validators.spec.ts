import { ServerSideValidators } from './server-side-validators';

describe('ServerSideValidators', () => {
  it('should create an instance', () => {
    expect(new ServerSideValidators()).toBeTruthy();
  });
});
